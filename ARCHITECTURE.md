# 架構說明

## 🏗️ Monorepo 架構圖

```
┌─────────────────────────────────────────────────────────────┐
│                    company-websites                         │
│                   (Turborepo Root)                          │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┴───────────────────┐
        │                                       │
        ▼                                       ▼
┌───────────────┐                    ┌──────────────────┐
│   apps/       │                    │   packages/      │
│               │                    │                  │
│ ├─ jianlin    │◄───────depends─────┤ ├─ api-template │
│ └─ banlong    │◄───────depends─────┤ └─ ui           │
│                                    │                  │
└───────────────┘                    └──────────────────┘
```

## 🚀 部署流程圖

### 方式 1: 獨立部署（推薦）

```
┌──────────────────────────────────────────────────────────┐
│              GitHub: company-websites                    │
│                (Single Repository)                       │
└──────────────────────────────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        │                           │
        ▼                           ▼
┌────────────────┐          ┌────────────────┐
│ Vercel Project │          │ Vercel Project │
│   "jianlin"    │          │   "banlong"    │
│                │          │                │
│ Root: apps/    │          │ Root: apps/    │
│       jianlin  │          │       banlong  │
└────────────────┘          └────────────────┘
        │                           │
        ▼                           ▼
┌────────────────┐          ┌────────────────┐
│ jianlin.com.tw │          │ banlong.com.tw │
└────────────────┘          └────────────────┘
```

### 方式 2: 提取給廠商

```
┌──────────────────────────────────────────────────────────┐
│              company-websites (Monorepo)                 │
└──────────────────────────────────────────────────────────┘
                      │
                      │ 執行提取腳本
                      ▼
┌──────────────────────────────────────────────────────────┐
│  ./scripts/extract-project.sh jianlin ~/Desktop/output  │
└──────────────────────────────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────────┐
│            jianlin-standalone/                           │
│            ├── app/                                      │
│            ├── components/                               │
│            ├── lib/                                      │
│            ├── packages/                                 │
│            │   └── api-template/  (內嵌)                │
│            ├── package.json       (獨立)                │
│            └── .env.local.example                        │
└──────────────────────────────────────────────────────────┘
                      │
                      │ 壓縮打包
                      ▼
┌──────────────────────────────────────────────────────────┐
│        jianlin-delivery-20250104.tar.gz                  │
│              (交付給廠商)                                │
└──────────────────────────────────────────────────────────┘
```

## 📦 依賴關係圖

```
apps/jianlin/
  ├── next: 16.0.1
  ├── react: 19.0.0
  ├── @repo/api-template: workspace:*  ←─┐
  └── axios: 1.7.9                       │
                                         │
packages/api-template/                   │
  ├── next: 16.0.1                       │
  └── axios: 1.7.9                       │
         ▲                               │
         └───────────────────────────────┘
```

## 🔄 開發工作流程

```
┌─────────────────────────────────────────────────────────┐
│                    開發者本地                           │
└─────────────────────────────────────────────────────────┘
                      │
                      │ git clone
                      ▼
┌─────────────────────────────────────────────────────────┐
│              company-websites/                          │
│              npm install                                │
└─────────────────────────────────────────────────────────┘
                      │
            ┌─────────┴─────────┐
            │                   │
            ▼                   ▼
┌──────────────────┐   ┌──────────────────┐
│ 修改 jianlin     │   │ 修改 api-template│
│ npm run dev      │   │ (自動 hot reload)│
│ --filter=jianlin │   │                  │
└──────────────────┘   └──────────────────┘
            │                   │
            └─────────┬─────────┘
                      │
                      │ git commit
                      ▼
┌─────────────────────────────────────────────────────────┐
│              Push to GitHub                             │
└─────────────────────────────────────────────────────────┘
                      │
            ┌─────────┴─────────┐
            │                   │
            ▼                   ▼
┌──────────────────┐   ┌──────────────────┐
│ Vercel 自動部署  │   │ Vercel 自動部署  │
│ jianlin.com.tw   │   │ banlong.com.tw   │
└──────────────────┘   └──────────────────┘
```

## 🎯 使用場景

### 場景 1: 新增一個公司網站

```bash
# 1. 建立新的 app
cd apps
cp -r jianlin newcompany

# 2. 修改 package.json
cd newcompany
# 修改 name, 更新依賴版本

# 3. 使用共用的 API template
# 已經自動依賴 @repo/api-template

# 4. 開發
npm run dev --filter=newcompany

# 5. 部署到 Vercel
# 建立新的 Vercel project，設定 Root Directory: apps/newcompany
```

### 場景 2: 更新共用的 API Template

```bash
# 1. 修改 packages/api-template
cd packages/api-template
# 修改 routes/contact.ts

# 2. 測試影響範圍
npm run build --filter=jianlin   # 測試建林
npm run build --filter=banlong   # 測試邦瓏

# 3. 提交
git add .
git commit -m "feat(api-template): 改進聯絡表單驗證"

# 4. 所有使用 api-template 的專案自動更新
```

### 場景 3: 交付專案給廠商

```bash
# 1. 提取專案
./scripts/extract-project.sh jianlin ~/Desktop/jianlin-delivery

# 2. 測試提取的專案
cd ~/Desktop/jianlin-delivery
npm install
npm run dev

# 3. 清理敏感資訊
rm .env.local
rm -rf .git

# 4. 建立交付文件
cat > HANDOVER.md << 'EOF'
# 交付說明
...
EOF

# 5. 壓縮打包
cd ..
tar -czf jianlin-delivery-$(date +%Y%m%d).tar.gz jianlin-delivery/
```

## 🔐 環境變數管理

```
┌────────────────────────────────────────────────────┐
│               開發環境                             │
│   apps/jianlin/.env.local                         │
│   - API_GATEWAY_API_KEY=dev_key                   │
│   - CONTACT_EMAIL_RECEIVERS=dev@example.com       │
└────────────────────────────────────────────────────┘
                      │
                      │ 不提交到 Git
                      ▼
┌────────────────────────────────────────────────────┐
│              Git Repository                        │
│   apps/jianlin/.env.local.example                 │
│   - API_GATEWAY_API_KEY=your_key_here             │
│   - CONTACT_EMAIL_RECEIVERS=your@email.com        │
└────────────────────────────────────────────────────┘
                      │
                      │ 部署到 Vercel
                      ▼
┌────────────────────────────────────────────────────┐
│           Vercel 生產環境                          │
│   Environment Variables (Web UI 設定)             │
│   - API_GATEWAY_API_KEY=prod_key_***              │
│   - CONTACT_EMAIL_RECEIVERS=info@jianlin.com.tw   │
└────────────────────────────────────────────────────┘
```

## 🛠️ 關鍵技術決策

### 為什麼選擇 Turborepo？

| 功能 | Turborepo | Lerna | Nx |
|------|-----------|-------|-----|
| 增量建置 | ✅ | ❌ | ✅ |
| Remote Cache | ✅ | ❌ | ✅ |
| 設定簡單 | ✅ | ⚠️ | ❌ |
| Vercel 整合 | ✅✅ | ⚠️ | ⚠️ |
| 學習曲線 | 低 | 中 | 高 |

### 為什麼分別部署而不是一起部署？

| 考量點 | 分別部署 | 一起部署 |
|--------|----------|----------|
| 網域管理 | ✅ 各自獨立 | ❌ 需要設定 rewrites |
| 環境變數 | ✅ 各自獨立 | ❌ 全部共用 |
| 部署影響 | ✅ 互不影響 | ❌ 一個失敗全失敗 |
| 權限管理 | ✅ 可分別授權 | ❌ 統一權限 |
| 交付給廠商 | ✅ 易於提取 | ❌ 需要拆分 |

### 為什麼使用 workspace 協議？

```json
// 開發階段 - 使用 workspace
{
  "dependencies": {
    "@repo/api-template": "workspace:*"
  }
}
// 優點：
// - npm install 時自動 link
// - 修改立即生效
// - 不需要 npm publish

// 提取階段 - 轉換為 file
{
  "dependencies": {
    "@repo/api-template": "file:./packages/api-template"
  }
}
// 優點：
// - 獨立專案可以直接 npm install
// - 不依賴 workspace 功能
// - 可以正常部署
```

## 📊 效能對比

### 建置時間對比

```
┌────────────────────────────────────────────────┐
│        傳統多個獨立專案                        │
│  jianlin:  30s                                 │
│  banlong:  30s                                 │
│  共用修改: 60s (兩個都要重建)                  │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│         Monorepo + Turborepo                   │
│  jianlin:      30s (首次)                      │
│  banlong:      30s (首次)                      │
│  共用修改:      35s (增量建置)                 │
│  使用 cache:    5s (命中快取)                  │
└────────────────────────────────────────────────┘
```

### 程式碼重用率

```
傳統方式:
apps/jianlin/lib/email/aws-ses.ts      (100 行)
apps/banlong/lib/email/aws-ses.ts      (100 行) ← 重複！
= 200 行程式碼

Monorepo 方式:
packages/api-template/routes/contact.ts (100 行)
apps/jianlin/app/api/contact/route.ts  (10 行) ← 只設定！
apps/banlong/app/api/contact/route.ts  (10 行) ← 只設定！
= 120 行程式碼

節省: 40% 程式碼 + 維護成本降低 50%
```

## 🎓 最佳實踐

### DO ✅

1. **共用程式碼放在 packages/**
   ```
   packages/
   ├── api-template/    # API 相關
   ├── ui/             # UI 元件
   └── utils/          # 工具函數
   ```

2. **每個 app 獨立部署**
   - 獨立的 Vercel project
   - 獨立的環境變數
   - 獨立的網域

3. **使用語義化版本**
   ```bash
   git commit -m "feat(api-template): 新增驗證碼功能"
   git commit -m "fix(jianlin): 修復聯絡表單 bug"
   ```

4. **定期清理未使用的 packages**
   ```bash
   npx depcheck
   ```

### DON'T ❌

1. **不要在 packages 中依賴 apps**
   ```
   ❌ packages/api-template 依賴 apps/jianlin
   ✅ apps/jianlin 依賴 packages/api-template
   ```

2. **不要共用環境變數檔案**
   ```
   ❌ .env.local (root)
   ✅ apps/jianlin/.env.local
   ✅ apps/banlong/.env.local
   ```

3. **不要把所有東西都放 packages**
   ```
   ❌ packages/jianlin-specific-logic/
   ✅ apps/jianlin/lib/specific-logic/
   ```

---

## 📞 技術支援

如有疑問，請參考：
- [DEPLOYMENT.md](./DEPLOYMENT.md) - 部署指南
- [Turborepo 文檔](https://turbo.build/repo/docs)
- [Vercel Monorepo](https://vercel.com/docs/concepts/monorepos)
